Allows you to define aliases as a scheme for your resources, for example define "data:" as a scheme which points to "file:/my/datastore".
It also predefines "tmp:" to be bound to your system temporary folder
